package Chapter17

object ex05 {

  // Why does RichInt implement Comparable[Int] and not Comparable[RichInt]?
  // I believe is because the implicit conversion would not kick in, because it would exist the comparison between RichInts

}
